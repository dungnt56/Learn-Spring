spring:
  datasource:
    url: jdbc:mysql://localhost:3306/unit_test?useSSL=false&serverTimezone=UTC
    username: root
    password: 56mysql02
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  application:
    name: Unit Test
logging:
  level:
    org.springframework.web: DEBUG
